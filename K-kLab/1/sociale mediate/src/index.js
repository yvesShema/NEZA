import react from 'react';
import ReactDOM from 'react-DOM';
import app from './app';
ReactDOM.render(
    <rect.strictMode>
        <app/>
    </rect.strictMode>,
    document.getElementById('root')
);